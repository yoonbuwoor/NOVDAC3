# Correctif Build 3.0.2

Le journal GitHub Actions signalait exactement deux problèmes :

1. `const_with_non_const` dans `lib/screens/home_screen.dart`.
2. `unused_local_variable` pour `imageHeightPx` dans `lib/screens/lab_hub_screen.dart`.

Les deux lignes ont été corrigées dans cette version.
